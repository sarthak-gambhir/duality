/** Spacing steps that map to the `--space-*` custom properties. */
export type SpaceStep = 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8;

/** Named size tokens that map to the `--size-*` custom properties. */
export type SizeToken = "sm" | "md" | "lg" | "xl";
