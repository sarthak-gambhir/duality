# PaletteSelect

A Select bound to the current palette, listing all named palettes.

## Props

| Prop         | Type               | Default   | Description                                              |
| ------------ | ------------------ | --------- | -------------------------------------------------------- |
| `align`      | `"end" \| "start"` | `start`   | Which edge the dropdown anchors to. Defaults to "start". |
| `aria-label` | `string`           | `Palette` | Accessible label. Defaults to "Palette".                 |
| `className`  | `string`           | -         |                                                          |
